{
  "name": "gravity-hoopers",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:client": "npm run dev --workspace=@gravity-hoopers/client",
    "dev:server": "npm run dev --workspace=@gravity-hoopers/server",
    "build": "npm run build --workspaces"
  }
}
